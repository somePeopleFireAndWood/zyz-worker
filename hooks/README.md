# Hooks

This directory is reserved for future hook definitions and hook scripts.

Document each hook with:

- trigger point
- inputs
- outputs
- failure behavior
- supported agents

Do not declare hooks in plugin manifests until the hook files and runtime behavior exist.
