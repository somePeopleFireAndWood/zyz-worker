# Skills

Each skill lives in its own directory and uses `SKILL.md` as the entry point.

Recommended layout:

```text
skills/
└── <skill-name>/
    ├── SKILL.md
    ├── references/
    └── templates/
```

Use `references/` for supporting material that should be loaded only when needed. Use `templates/` for reusable document or output formats.
